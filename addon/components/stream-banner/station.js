import Component from '@ember/component';
import layout from '../../templates/components/stream-banner/station';

export default Component.extend({
  layout,
  classNames: ['stream-banner-station', 'nudge']
});
