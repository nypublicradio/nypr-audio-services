import Component from '@ember/component';
import layout from '../../templates/components/stream-banner/controls';

export default Component.extend({
  layout,
  classNames: ['stream-banner-controls']
});
