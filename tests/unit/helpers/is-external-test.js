
import { isExternal } from 'dummy/helpers/is-external';
import { module, test } from 'qunit';

module('Unit | Helper | is external');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isExternal([42]);
  assert.ok(result);
});

